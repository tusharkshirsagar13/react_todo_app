# React + TypeScript Todo App

A fast and responsive Todo application built to demonstrate modern React development practices using TypeScript and Vite.

## 🚀 Features

* **Tech Stack:** Built with React (Hooks), TypeScript, and Vite for blazing fast performance.
* **State Management:** Uses React Context API for global state management.
* **Persistent Data:** Tasks are saved to the browser's LocalStorage, so data is not lost on refresh.
* **Routing & Filtering:** specialized routing to filter tasks by "All", "Active", and "Completed" statuses.
* **Styling:** Clean and responsive UI using standard CSS.

## 🛠️ Installation & Setup

1.  Clone the repository.
2.  Run `npm install` to install dependencies.
3.  Run `npm run dev` to start the local development server.
